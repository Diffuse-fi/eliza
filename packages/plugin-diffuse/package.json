
{
    "name": "@elizaos/plugin-diffuse",
    "version": "0.1.7",
    "description": "diffuse plugin for ElizaOS",
    "main": "dist/index.js",
    "type": "module",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsup src/index.ts --format esm --dts",
        "lint": "eslint --fix --cache .",
        "watch": "tsc --watch",
        "dev": "tsup src/index.ts --format esm --dts --watch"
    },
    "dependencies": {
        "@elizaos/core": "workspace:*",
        "@diffuse-fi/fetch-proof-client": "^0.0.1"
    },
    "devDependencies": {
        "tsup": "^8.3.5"
    }
}